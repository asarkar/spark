sudo: required

language: scala

services:
  - docker

before_script: cd yelp-dataload

script:
  - sbt clean docker:publishLocal

after_success:
  - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
  - docker push asarkar/yelp-dataload
